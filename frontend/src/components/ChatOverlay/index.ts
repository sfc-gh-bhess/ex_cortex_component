/**
 * ChatOverlay component for Cortex Agent interactions
 * Can be imported and used in other React applications
 */

export { ChatOverlay } from './ChatOverlay';
export type { ChatOverlayProps, ChatMessage, DisplayConfig } from './types';
export { defaultDisplayConfig } from './types';

